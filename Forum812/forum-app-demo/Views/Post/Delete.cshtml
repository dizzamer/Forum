@model Forum.Web.Models.Post.DeletePostModel

<div class="container body-content">
    @if (!Context.User.Identity.IsAuthenticated)
    {
        <div class="row sectionHeader">
            <div class="sectionHeading">Delete Post</div>
            <div class="sectionDescription">
                <p>
                   Вы действительно хотите удалить этот пост?
                </p>
            </div>
        </div>
        <div class="deletePost">
            <button id="deleteButton" class="btn btn-lg btn-danger">Подтвердить</button>
            <button id="deleteButton" class="btn btn-lg btn-default">Отмена</button>
        </div>
    }
</div>
